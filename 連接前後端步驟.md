# 🔗 連接前端與後端 - 完成部署

## ✅ 當前狀態

- ✅ 前端已部署到 GitHub Pages
- ✅ 後端已部署到 Railway（服務正在運行）

## 📋 接下來需要完成的步驟

### 步驟 1：獲取 Railway 後端 URL（2 分鐘）

1. **在 Railway 中**：
   - 點擊您的服務 `ai-nutritionist-tracker`
   - 點擊 **"Settings"** 標籤
   - 找到 **"Domains"** 區塊
   - 如果沒有域名，點擊 **"Generate Domain"**
   - **複製這個 URL**（例如：`https://ai-nutritionist-tracker-production-xxxx.up.railway.app`）

2. **測試後端是否正常**：
   - 在瀏覽器打開：``https://您的-railway-url.up.railway.app/
   - 應該會看到 JSON 回應：`{"status":"ok","message":"Server is running",...}`

---

### 步驟 2：配置 Railway 環境變數（3 分鐘）

1. **在 Railway 中**：
   - 點擊服務 `ai-nutritionist-tracker`
   - 點擊 **"Variables"** 標籤
   - 點擊 **"New Variable"**

2. **添加以下環境變數**：

   | 變數名稱 | 值 | 說明 |
   |---------|-----|------|
   | `API_KEY` | 您的 Google Gemini API Key | 從 Google AI Studio 取得 |
   | `JWT_SECRET` | 任意字串（如：`mysecretkey123456789`） | 用於 JWT 簽名，建議使用長字串 |
   | `NODE_ENV` | `production` | 生產環境標記 |

3. **保存後，Railway 會自動重新部署**

---

### 步驟 3：在 GitHub 設置 API URL Secret（2 分鐘）

1. **前往 GitHub 倉庫**：
   - https://github.com/Lethe410/ai-nutritionist-tracker

2. **添加 Secret**：
   - 點擊 **"Settings"** → **"Secrets and variables"** → **"Actions"**
   - 點擊 **"New repository secret"**
   - **Name**: `VITE_API_URL`
   - **Value**: `https://您的-railway-url.up.railway.app/api`
     - ⚠️ **重要**：URL 後面必須加上 `/api`
     - 例如：`https://ai-nutritionist-tracker-production-xxxx.up.railway.app/api`
   - 點擊 **"Add secret"**

---

### 步驟 4：重新部署前端（2 分鐘）

1. **觸發重新部署**：
   - 前往：https://github.com/Lethe410/ai-nutritionist-tracker/actions
   - 點擊左側 **"Deploy to GitHub Pages"**
   - 點擊右側 **"Run workflow"** 按鈕
   - 選擇 `main` 分支
   - 點擊綠色的 **"Run workflow"** 按鈕

2. **等待部署完成**（約 2-3 分鐘）

---

### 步驟 5：測試應用（1 分鐘）

1. **訪問應用**：
   - https://lethe410.github.io/ai-nutritionist-tracker/

2. **測試功能**：
   - ✅ 註冊/登入帳號
   - ✅ 上傳食物照片分析
   - ✅ AI 聊天功能
   - ✅ 查看營養日記

---

## 🎉 完成！

如果所有步驟都完成，您的應用應該可以正常運作了！

## ❓ 如果遇到問題

### 問題 1：無法連接到後端
- **檢查**：Railway 服務是否正在運行（綠色 "Active" 狀態）
- **檢查**：GitHub Secret `VITE_API_URL` 是否正確設置
- **檢查**：URL 後面是否有 `/api`

### 問題 2：AI 功能無法使用
- **檢查**：Railway 環境變數 `API_KEY` 是否正確設置
- **檢查**：Google Gemini API Key 是否有效

### 問題 3：登入失敗
- **檢查**：Railway 環境變數 `JWT_SECRET` 是否設置
- **檢查**：後端日誌是否有錯誤訊息

---

## 📱 手機訪問

完成後，您可以在手機上訪問：
```
https://lethe410.github.io/ai-nutritionist-tracker/
```

可以添加到主畫面，作為 PWA 應用使用！

