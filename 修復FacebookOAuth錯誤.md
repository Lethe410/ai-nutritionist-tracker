# 🔧 修復 Facebook OAuth 錯誤

## ❌ 遇到的錯誤

錯誤訊息：
```
網址已遭封鎖
這個重新導向失敗了，因為重新導向URI並未列入應用程式用戶端 OAuth 設定的許可名單中。
```

**原因**：Facebook 登入需要正確的 OAuth 重新導向 URI。

---

## ✅ 解決方法

### 步驟 1：添加 Firebase OAuth Redirect URI

在 Facebook Developers 的「有效的 OAuth 重新導向 URI」中，您需要添加：

```
https://nutriai-410.firebaseapp.com/__/auth/handler
```

**格式**：`https://[您的項目ID].firebaseapp.com/__/auth/handler`

**您的項目 ID**：`nutriai-410`

### 步驟 2：添加 GitHub Pages URI（如果使用）

如果您的應用部署在 GitHub Pages，也需要添加：

```
https://lethe410.github.io/__/auth/handler
```

**注意**：GitHub Pages 的 URI 格式可能不同，Firebase 通常使用 `firebaseapp.com` 域名。

---

## 📋 完整的 URI 列表

在 Facebook Developers 中，應該添加以下 URI：

1. **Firebase 主要 URI**（必須）：
   ```
   https://nutriai-410.firebaseapp.com/__/auth/handler
   ```

2. **本地開發 URI**（可選）：
   ```
   http://localhost:5173/__/auth/handler
   ```

3. **GitHub Pages URI**（如果使用）：
   ```
   https://lethe410.github.io/__/auth/handler
   ```

---

## 🔍 如何找到正確的 URI

### 方法 1：從 Firebase Console

1. 前往 Firebase Console → Authentication → 登入方法 → Facebook
2. 查看「授權網域」或「重新導向 URI」
3. 應該會顯示：`https://nutriai-410.firebaseapp.com`

### 方法 2：從錯誤訊息

Firebase 會自動使用以下格式：
```
https://[項目ID].firebaseapp.com/__/auth/handler
```

---

## 📝 在 Facebook Developers 中添加 URI

### 步驟 1：進入設置

1. 在 Facebook Developers 中，選擇您的應用
2. 點擊「**Facebook 登入**」→「**設定**」

### 步驟 2：添加 URI

1. 在「**有效的 OAuth 重新導向 URI**」區塊
2. 點擊「**新增 URI**」或直接在輸入框中輸入
3. 添加：
   ```
   https://nutriai-410.firebaseapp.com/__/auth/handler
   ```
4. 點擊「**儲存變更**」

### 步驟 3：啟用必要的設置

確保以下設置已啟用：

1. **「使用 JavaScript SDK 登入」**：
   - 應該設置為「**是**」（開啟）

2. **「從裝置登入」**（可選）：
   - 可以保持「否」

---

## ✅ 完成檢查清單

- [ ] ✅ 已在 Facebook Developers 中添加 `https://nutriai-410.firebaseapp.com/__/auth/handler`
- [ ] ✅ 已啟用「使用 JavaScript SDK 登入」
- [ ] ✅ 已在 Firebase Console 中配置 Facebook（App ID 和 App Secret）
- [ ] ✅ 已測試 Facebook 登入

---

## 🧪 測試

添加 URI 後：

1. **等待幾秒鐘**讓設置生效
2. **清除瀏覽器緩存**
3. **重新測試 Facebook 登入**
4. **應該不會再出現錯誤**

---

## 🐛 如果還是有問題

### 檢查項目

1. **確認項目 ID 正確**：
   - 在 Firebase Console → 專案設定 → 一般設定
   - 確認項目 ID 是 `nutriai-410`

2. **確認 URI 完全匹配**：
   - URI 必須完全一致，包括 `https://` 和結尾的 `/__/auth/handler`
   - 不能有多餘的空格或字符

3. **確認設置已保存**：
   - 在 Facebook Developers 中確認 URI 已保存
   - 等待幾秒鐘讓設置生效

---

## 🚀 完成後

添加 URI 並保存後，告訴我：
- ✅ "我已經添加了 URI"
- ✅ 或告訴我遇到的問題

然後我會幫您測試和解決問題！

