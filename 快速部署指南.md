# 🚀 快速部署指南

## ✅ 第一步：代碼已準備好！

您的代碼已經提交到本地 Git，現在需要推送到 GitHub。

---

## 📝 第二步：在 GitHub 創建倉庫

### 2.1 創建新倉庫

1. 前往：https://github.com/new
2. **Repository name**: `ai-nutritionist-tracker`
3. **Description**（可選）: `AI 營養助手 - 智能飲食追蹤應用`
4. **選擇 Public**（GitHub Pages 需要公開倉庫）
5. **不要勾選**以下選項：
   - ❌ Add a README file
   - ❌ Add .gitignore
   - ❌ Choose a license
6. 點擊 **"Create repository"**

### 2.2 推送代碼

創建倉庫後，在終端執行：

```bash
git push -u origin main
```

如果遇到認證問題，可能需要：
- 使用 GitHub Personal Access Token 作為密碼
- 或使用 SSH 方式連接

---

## 🔧 第三步：部署後端到 Railway

### 3.1 註冊並登入 Railway

1. 前往：https://railway.app/
2. 點擊 **"Start a New Project"**
3. 選擇 **"Login with GitHub"**
4. 授權 Railway 訪問您的 GitHub

### 3.2 部署後端服務

1. 在 Railway 儀表板，點擊 **"New Project"**
2. 選擇 **"Deploy from GitHub repo"**
3. 選擇您的 `ai-nutritionist-tracker` 倉庫
4. Railway 會自動開始部署

### 3.3 設置環境變數

1. 點擊部署的服務
2. 點擊 **"Variables"** 標籤
3. 點擊 **"New Variable"**，添加：

| 變數名稱 | 值 | 說明 |
|---------|-----|------|
| `API_KEY` | 您的 Google Gemini API Key | 從 https://makersuite.google.com/app/apikey 取得 |
| `JWT_SECRET` | 任意字串 | 例如：`mysecretkey123456` |
| `NODE_ENV` | `production` | 生產環境標記 |

**重要**：`PORT` 變數 Railway 會自動設置，不需要手動添加。

### 3.4 獲取後端 URL

1. 部署完成後，點擊服務
2. 點擊 **"Settings"** 標籤
3. 找到 **"Domains"** 區塊
4. 點擊 **"Generate Domain"**
5. **複製這個 URL**（例如：`https://ai-nutritionist-tracker-production-xxxx.up.railway.app`）

---

## 🌐 第四步：配置前端 API URL

### 4.1 在 GitHub 設置 Secret

1. 前往您的 GitHub 倉庫：https://github.com/Lethe410/ai-nutritionist-tracker
2. 點擊 **"Settings"**（倉庫設置）
3. 左側選單找到 **"Secrets and variables"** → **"Actions"**
4. 點擊 **"New repository secret"**
5. 添加：
   - **Name**: `VITE_API_URL`
   - **Value**: `https://您的-railway-url.up.railway.app/api`
     - 將 `您的-railway-url` 替換為步驟 3.4 獲取的 URL
     - **注意**：URL 後面要加上 `/api`
6. 點擊 **"Add secret"**

---

## 📦 第五步：啟用 GitHub Pages

### 5.1 設置 GitHub Pages

1. 在 GitHub 倉庫中，點擊 **"Settings"**
2. 左側選單找到 **"Pages"**
3. 在 **"Source"** 下選擇：**GitHub Actions**
4. 點擊 **"Save"**

### 5.2 觸發部署

1. 點擊倉庫頂部的 **"Actions"** 標籤
2. 您會看到 **"Deploy to GitHub Pages"** 工作流
3. 點擊 **"Run workflow"** 按鈕（右側）
4. 選擇 `main` 分支
5. 點擊綠色的 **"Run workflow"** 按鈕
6. 等待部署完成（約 2-3 分鐘）

### 5.3 查看部署結果

1. 部署完成後，前往 **"Settings"** → **"Pages"**
2. 您會看到網址：`https://lethe410.github.io/ai-nutritionist-tracker/`

---

## 📱 第六步：在手機上訪問

### 6.1 獲取網址

您的應用網址：
```
https://lethe410.github.io/ai-nutritionist-tracker/
```

### 6.2 手機訪問

1. 打開手機瀏覽器（Chrome、Safari 等）
2. 輸入上述網址
3. 應該可以正常使用！

### 6.3 安裝為 PWA（可選）

- **Android**: 瀏覽器選單 → "安裝應用程式" 或 "加到主畫面"
- **iOS**: 分享按鈕 → "加入主畫面"

---

## ⚠️ 重要提醒

### 更新倉庫名稱（如果需要）

如果您的 GitHub 倉庫名稱不是 `ai-nutritionist-tracker`，請修改 `vite.config.ts` 第 7 行：

```typescript
base: process.env.NODE_ENV === 'production' ? '/您的倉庫名稱/' : '/',
```

### 設置 CORS（如果遇到跨域問題）

在 Railway 環境變數中添加：
- **Name**: `ALLOWED_ORIGINS`
- **Value**: `https://lethe410.github.io`

---

## 🎉 完成！

部署完成後，您就可以：
- ✅ 在任何地方用手機訪問應用
- ✅ 分享給朋友使用
- ✅ 隨時更新和改進應用

---

## 🔄 更新應用

當您修改代碼後：

```bash
git add .
git commit -m "更新描述"
git push
```

前端會自動重新部署，後端在 Railway 也會自動更新。

---

## ❓ 遇到問題？

### 問題 1：Git push 失敗

**解決方法**：
- 確認 GitHub 倉庫已創建
- 檢查倉庫 URL 是否正確
- 使用 GitHub Personal Access Token 作為密碼

### 問題 2：Railway 部署失敗

**解決方法**：
- 檢查環境變數是否正確設置
- 查看 Railway 的日誌確認錯誤
- 確認 `API_KEY` 有效

### 問題 3：前端無法連接後端

**解決方法**：
- 確認 GitHub Secret `VITE_API_URL` 已設置
- 確認 URL 格式正確（結尾有 `/api`）
- 檢查 Railway 後端是否正在運行

### 問題 4：GitHub Pages 404

**解決方法**：
- 確認 `vite.config.ts` 中的 `base` 路徑正確
- 等待幾分鐘讓 GitHub Pages 更新
- 檢查 GitHub Actions 部署是否成功

